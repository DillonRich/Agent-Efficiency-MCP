<!-- PROMPTMCP_META
provider: deepseek
model: deepseek-v4-flash
archetype: vague
archetype_tags: messy_polite,vague,follow_up,multi_goal
input_chars: 598
blueprint_chars: 2125
compression_ratio: 3.554
context_bytes: 2048
context_budget: standard
known_paths: 13
git_changed_files: 3
forced_files: 0
forced_media: 0
vision_images: 0
research_urls: 0
directives: (none)
repair_pass: no
-->
# 🎯 Current Task Blueprint: ACTIVE

## 1. Absolute Objective
Refactor `src/engine.ts` to reduce token waste in context-gathering logic while keeping all existing behavior unchanged.

## 2. Technical Requirements & Boundary Rules
- Must preserve every existing exported function signature and behavior – zero regressions.
- Token waste reduction techniques: strip leading/trailing whitespace from assembled context strings, deduplicate repeated file path references, collapse repeated delimiter lines (e.g., `---`), compress multiple blank lines into one.
- No changes to `src/server.ts`, `README.md`, docs, templates, scripts, or install flow.
- No new features, environment variables, or configuration options.
- Use only the TypeScript/Node standard library – no additional dependencies.
- Non-goals: README polish, server modifications, context schema changes, package.json updates, install flow improvements, or any feature additions.
- Conflict: User wants "everything better" – we strictly limit to engine token efficiency; all other improvements are deferred.

## 3. Targeted Codebase Vectors
- `src/engine.ts` -> primary edit target (context assembly logic)
- `src/context.ts` -> read-only reference (understand current context gathering flow)
- `src/validate.ts` -> read-only reference (may share validation utilities)
- `package.json` -> forced file; read-only (verify build/test scripts exist)
- `src/server.ts` -> forced file; read-only (only to confirm no changes needed here)

## 4. Verification Checkpoints
- [ ] `npx tsc --noEmit` passes with zero errors.
- [ ] Manual inspection: running the current context generation (e.g., `npx tsx src/engine.ts` test scenario) produces output with fewer total tokens than before, and all original content semantics are preserved.
- [ ] No existing tests (if any) fail when run (e.g., `npm test` – adjust if test script absent).

---
👉 **Awaiting Your Approval:**
If this blueprint accurately maps your intention, reply with **"GO"** in the chat window.
If you want adjustments, modify this markdown file directly or type your adjustments in the chat before executing.
