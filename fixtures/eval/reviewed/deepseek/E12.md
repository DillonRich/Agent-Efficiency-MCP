<!-- PROMPTMCP_META
provider: deepseek
model: deepseek-v4-flash
archetype: multi_goal
archetype_tags: follow_up,multi_goal
input_chars: 170
blueprint_chars: 2100
compression_ratio: 12.353
context_bytes: 2048
context_budget: standard
known_paths: 13
git_changed_files: 3
forced_files: 0
forced_media: 0
vision_images: 0
research_urls: 0
directives: (none)
repair_pass: no
-->
# 🎯 Current Task Blueprint: ADD RETRY LOGIC TO HTTP CALLS

## 1. Absolute Objective
Implement retry logic with exponential backoff for all outbound HTTP requests made in the PromptMCP server, modifying the relevant client code in `src/server.ts` and any shared utility file.

## 2. Technical Requirements & Boundary Rules
- Must implement exponential backoff with configurable max retries (default 3) and initial delay (default 1s).
- Must only retry on server errors (5xx) and network/timeout errors; never retry 4xx responses except 429 (rate-limit) which should use Retry-After header.
- Must log each retry attempt with attempt number and reason (via console.warn or logger if present).
- Must not add new external dependencies – use only Node.js built-in `setTimeout` and promises.
- Must preserve existing behavior for non-retry responses and error propagation after exhausting retries.
- Non-goals (deferred to future GO cycles): redesign README, migrate license text, add Cohere provider, refactor CLI help text.
- Forced files `package.json` and `src/server.ts` must be referenced or modified.

## 3. Targeted Codebase Vectors
- `package.json` -> read-only: verify existing dependencies (no axios/needle dependency assumed – use native http/https).
- `src/server.ts` -> edit: locate where outgoing HTTP requests are made (likely in MCP transport or tool execution); wrap that call with retry logic.
- `src/engine.ts` -> read-only: if HTTP calls exist there, adjust accordingly; otherwise leave untouched.

## 4. Verification Checkpoints
- [ ] `npm run typecheck` passes without errors.
- [ ] New unit test (if test runner present) or manual script confirms: first request fails 3 times → final error is thrown; request fails once → succeeds on retry.
- [ ] No changes to `README.md`, no new provider code, no license migration, CLI help unchanged.

---
👉 **Awaiting Your Approval:**
If this blueprint accurately maps your intention, reply with **"GO"** in the chat window. 
If you want adjustments, modify this markdown file directly or type your adjustments in the chat before executing.
