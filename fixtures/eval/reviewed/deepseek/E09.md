<!-- PROMPTMCP_META
provider: deepseek
model: deepseek-v4-flash
archetype: contradictory
archetype_tags: messy_polite,follow_up,overconstrained,contradictory
input_chars: 259
blueprint_chars: 2105
compression_ratio: 8.127
context_bytes: 2048
context_budget: standard
known_paths: 13
git_changed_files: 3
forced_files: 0
forced_media: 0
vision_images: 0
research_urls: 0
directives: (none)
repair_pass: no
-->
# 🎯 Current Task Blueprint: Refactor Engine with Dependency Injection

## 1. Absolute Objective
Refactor `src/engine.ts` to accept dependencies via constructor/function injection, leaving provider implementations untouched and adding zero new tests.

## 2. Technical Requirements & Boundary Rules
- CONFLICT: Prompt says "never add tests" then "must add full test suite". We honor the explicit negative: do **not** add or modify any test files.
- Replace direct instantiation of providers in `src/engine.ts` with injected provider factories or instances.
- Keep existing provider modules (under `src/providers/` if they exist) completely unchanged – no edits to any provider file.
- No new cloud provider support – ignore "support every cloud provider".
- No UI, styling, or "" changes.
- Non-goals: adding test suite, adding new providers, rewriting entire codebase, touching README or docs (unless needed for integration), performance optimization.
- The refactor must compile with `npm run typecheck` and `npm run build` (if defined) without errors.

## 3. Targeted Codebase Vectors
- `package.json` -> read dev/build scripts and dependency list (read-only reference)
- `src/server.ts` -> inspect how `engine.ts` is currently imported and used (read-only reference)
- `src/engine.ts` -> **edit**: apply dependency injection pattern (constructor injection or factory function)
- `src/context.ts` -> read if it interacts with engine (read-only reference; optional)
- `src/validate.ts` -> read if it interacts with engine (read-only reference; optional)
- `scripts/` -> read build/test scripts (read-only reference)

## 4. Verification Checkpoints
- [ ] `npm run typecheck` passes with zero errors.
- [ ] `npm run build` (or `tsc --noEmit`) passes with zero errors.
- [ ] Running `node dist/server.js` (or equivalent) starts without crashing – manual smoke test.

---
👉 **Awaiting Your Approval:** If this blueprint accurately maps your intention, reply with **"GO"** in the chat window. If you want adjustments, modify this markdown file directly or type your adjustments in the chat before executing.
