<!-- PROMPTMCP_META
provider: deepseek
model: deepseek-v4-flash
archetype: vague
archetype_tags: vague
input_chars: 107
blueprint_chars: 1502
compression_ratio: 14.037
context_bytes: 2048
context_budget: standard
known_paths: 13
git_changed_files: 3
forced_files: 0
forced_media: 0
vision_images: 0
research_urls: 0
directives: (none)
repair_pass: no
-->
# 🎯 Current Task Blueprint: Polish MCP server initialization and error handling

## 1. Absolute Objective
Strengthen the MCP server startup and error handling in `src/server.ts` so that initialization fails gracefully with clear diagnostics instead of silent crashes.

## 2. Technical Requirements & Boundary Rules
- Must not change the core MCP SDK integration pattern — only wrap existing calls with structured error handling.
- Must preserve the existing `dotenv` and `zod` usage for environment validation.
- Must not add new routes, tools, or resources beyond what is already in `src/server.ts`.
- Non-goals: full codebase refactor, introducing new features, rewriting `src/engine.ts`, updating documentation.

## 3. Targeted Codebase Vectors
- `src/validate.ts` -> read-only reference: if present, note validation routines that should be called during startup.

## 4. Verification Checkpoints
- [ ] `npm run dev` starts without any uncaught exceptions or silent exits (observable output from server process).
- [ ] If forced error (e.g., missing `MCP_TOKEN` env), server exits with a descriptive message including the missing variable name.

---
👉 **Awaiting Your Approval:**
If this blueprint accurately maps your intention, reply with **"GO"** in the chat window.
If you want adjustments, modify this markdown file directly or type your adjustments in the chat before executing.
