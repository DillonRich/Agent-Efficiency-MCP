<!-- PROMPTMCP_META
provider: deepseek
model: deepseek-v4-flash
archetype: vague
archetype_tags: vague
input_chars: 18
blueprint_chars: 1979
compression_ratio: 109.944
context_bytes: 2048
context_budget: standard
known_paths: 13
git_changed_files: 3
forced_files: 0
forced_media: 0
vision_images: 0
research_urls: 0
directives: (none)
repair_pass: no
-->
# 🎯 Current Task Blueprint: Fix Immediate Breakage

## 1. Absolute Objective
Identify and resolve the compilation or runtime error that prevents the MCP server from starting, using the forced files and recent changes as the primary context.

## 2. Technical Requirements & Boundary Rules
- **Must** first check if the project type-checks with `npx tsc --noEmit`; fix any TypeScript errors found.
- **Must** then attempt to start the server with `npx tsx src/server.ts`; capture any runtime errors and fix them.
- **Must** only modify files in the following set: `package.json`, `src/server.ts`, `src/engine.ts`, `src/context.ts`, `src/validate.ts` (if needed to fix errors).
- **Must not** change any styling, documentation, or add new dependencies.
- Non-goals: no refactoring, no adding features, no exploring `templates/`, `scripts/`, or `docs/` unless paths are directly referenced in error messages.
- CONFLICT: The user says "fix it" without specifying – prioritise the most recent change (commits "polish context" and "add directives") as the likely breakage cause.

## 3. Targeted Codebase Vectors
- `package.json` -> inspect scripts and dependency versions (read‑only reference).
- `src/server.ts` -> edit to fix startup issues; mirror pattern in `src/server.ts` if broken.
- `src/engine.ts` -> read‑only to understand context processing (recently modified).
- `README.md` -> read‑only (project doc; optional).

## 4. Verification Checkpoints
- [ ] `npx tsc --noEmit` exits with exit code 0 (no type errors).
- [ ] `npx tsx src/server.ts` starts the server process without crashing and logs an "MCP server listening" message.
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (or add a focused assertion for this change)

---
👉 **Awaiting Your Approval:**
If this blueprint accurately maps your intention, reply with **"GO"** in the chat window.
If you want adjustments, modify this markdown file directly or type your adjustments in the chat before executing.
